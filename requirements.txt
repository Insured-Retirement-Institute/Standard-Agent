# IRI Standards Agent v1.0 — Python dependencies
# Install: pip install -r requirements.txt

# Core
strands-agents
databricks-sdk
PyYAML

# Server
fastapi
uvicorn[standard]
pydantic

# Optional (Data Dictionary Excel parsing)
openpyxl

# Dev only (not required for production)
# python-dotenv
